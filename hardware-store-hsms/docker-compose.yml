version: '3.8'
services:
  backend:
    build: ./src/backend
    ports:
      - "5000:80"

  frontend:
    build: ./src/frontend
    ports:
      - "3000:3000"

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
